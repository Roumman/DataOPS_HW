[project]
name = "db-migrations"
version = "0.1.0"
description = "Database migrations with yoyo-migrations"
requires-python = ">=3.9"
dependencies = [
    "yoyo-migrations[postgres]>=9.0.0",
]

[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"
